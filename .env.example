# Option 1: Vertex AI via Cloud Function (recommended — uses GCP free credits)
# Deploy the cloud-function/ directory, then set the URL here:
VITE_CLOUD_FUNCTION_URL=https://us-central1-ff-lady-vampire-studio.cloudfunctions.net/generate

# Option 2: Direct Gemini API (fallback — requires Generative Language API enabled)
# VITE_GEMINI_API_KEY=your_gemini_api_key_here
